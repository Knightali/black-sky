                # BREAK STATEMENT => Use to terminate a loop
                                   # Used in a loop structure

for a in range(8):   
    print(a)
    if a==4:  
        break           #it will Break or stop the loop when the value of a reaches 4

                # And If there is an else than it will not execute because the termination is for loop is not successful 
else:                   #If it will exit because of break than else is not executed
                        #If it will exit  not because of break than else is  executed
    print("Done")
